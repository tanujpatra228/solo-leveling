# deps
node_modules/
.pnpm-store/

# build
dist/
.vite/
*.tsbuildinfo

# secrets — never commit
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
*.pem
vapid.json

# cloudflare
.wrangler/
# worker-configuration.d.ts IS committed: CI runs a typecheck and cannot type
# the Worker without it. It holds binding names and runtime types only, never
# a secret. Regenerate with `pnpm run cf:types` after changing wrangler.jsonc.

# editor / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# test / coverage
coverage/
